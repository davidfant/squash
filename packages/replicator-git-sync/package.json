{
  "name": "replicator-git-sync",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "_dev": "wrangler dev --port 8888 --show-interactive-dev-session=false",
    "postinstall": "uv sync",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.28.1"
  }
}
