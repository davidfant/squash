{
  "name": "@squashai/deployment-proxy",
  "type": "module",
  "version": "0.0.0",
  "scripts": {
    "dev": "pnpm typegen && wrangler dev",
    "push": "wrangler deploy --minify --env production",
    "typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "devDependencies": {
    "wrangler": "^4.40.0"
  }
}
