{
  "name": "@squashai/preview-proxy",
  "type": "module",
  "version": "0.0.0",
  "scripts": {
    "_dev": "pnpm typegen && wrangler dev",
    "_push": "wrangler deploy --minify --env production",
    "typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "devDependencies": {
    "wrangler": "^4.31.0"
  }
}
