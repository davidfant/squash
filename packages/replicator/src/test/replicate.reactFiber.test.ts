describe("replicate > reactFiber", () => {
  xit("should create a component", () => {});
  xit("should only create one component for memo(forwardRef(function))", () => {});
  xit("should not create a component when returning null", () => {});
  xit("should create component when one instance returns null and another returns jsx", () => {});

  describe("naming", () => {
    xit("should rewrite component", () => {});
    xit("should rewrite component bottoms up", () => {});
  });
});
