{
  "name": "@squashai/cli",
  "version": "0.0.8-alpha.3",
  "bin": {
    "squash": "./bin/squash.js"
  },
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "tsc && chmod +x bin/squash.js",
    "dev": "tsc --watch --preserveWatchOutput",
    "prepublishOnly": "npm run build",
    "push": "npm publish || echo 'Already published, skipping'"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.1",
    "commander": "^14.0.1",
    "zod": "^4.0.16"
  },
  "devDependencies": {
    "@types/node": "^22.8.7",
    "typescript": "~5.8.3"
  },
  "files": [
    "dist",
    "bin",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  }
}
